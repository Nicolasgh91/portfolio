---
interface Props {
  tags: string[];
  category: string;
}

const { tags, category } = Astro.props;
---

<footer class="mt-12 border-t border-[color:var(--border-subtle)] pt-8">
  <a
    href={`/blog/categoria/${category}`}
    class="mb-4 inline-block text-xs uppercase tracking-widest text-[var(--text-muted)] no-underline transition-colors hover:text-[var(--color-accent-400)]"
  >
    {category}
  </a>

  <div class="flex flex-wrap gap-2">
    {tags.map((tag) => (
      <a
        href={`/blog/etiqueta/${tag}`}
        class="inline-flex items-center rounded-full border border-[color:var(--border-subtle)] bg-[var(--bg-tertiary)]/50 px-3 py-1 text-xs text-[var(--text-secondary)] no-underline transition-all duration-200 hover:border-[color:var(--color-accent-400)]/30 hover:text-[var(--color-accent-400)]"
      >
        #{tag}
      </a>
    ))}
  </div>
</footer>
